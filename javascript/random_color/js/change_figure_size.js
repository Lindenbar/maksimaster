// "figure" from document "js/set_default_figure_size.js"

let change_figure_width = (el) => {
    figure.style.width = `${el.value}px`;
};

let change_figure_height = (el) => {
    figure.style.height = `${el.value}px`;
};